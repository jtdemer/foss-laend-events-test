
- name: Basic checks (robust, no inline quotes)
  shell: bash
  run: |
    node <<'NODE'
    const fs = require('fs');

    // 1) event.json laden
    let raw = fs.readFileSync('event.json','utf8');
    let data;
    try {
      data = JSON.parse(raw);
    } catch (e) {
      // Falls doppelt serialisiert (z. B. "\"{...}\"")
      try {
        data = JSON.parse(JSON.parse(raw));
      } catch (e2) {
        console.error('❌ Konnte event.json nicht parsen:', e2.message);
        process.exit(1);
      }
    }

    // 2) Pflichtfelder passend zu EN-Labels in event.yml:
    const required = [
      'title',
      'description',
      'start',
      'timezone',
      'location',
      'city',
      'category',
      'organizer',
      'url'
    ];

    // 3) Textwert aus Parser-Objekten holen (.text oder content[])
    const getText = (v) => {
      if (typeof v === 'string') return v.trim();
      if (v && typeof v === 'object') {
        if (typeof v.text === 'string') return v.text.trim();
        if (Array.isArray(v.content)) return v.content.join(' ').trim();
      }
      return '';
    };

    // 4) Felder prüfen
    const missing = required.filter(k => !(k in data) || !getText(data[k]));
    if (missing.length) {
      console.error('❌ Fehlende Felder:', missing);
      process.exit(1);
    }

    // 5) Einverständnis/Consent prüfen (id: consent oder terms)
    const terms = data['consent'] || data['terms'];
    let consentOK = false;
    if (terms && typeof terms === 'object') {
      if (Array.isArray(terms.list)) {
        consentOK = terms.list.some(it => it.checked === true);
      } else {
        const t = getText(terms);
        consentOK = /\[x\]/i.test(t);
      }
    }
    if (!consentOK) {
      console.error('❌ Einverständnis/Consent nicht bestätigt.');
      process.exit(1);
    }

    console.log('✅ Validierung OK');
    NODE
